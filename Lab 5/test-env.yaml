parameter_defaults:
  InstanceType: m1.nano

parameters:
  KeyName: test_key
  InstanceType: m1.nano
  ImageId: Cirros

parameter_merge_strategies:
  default: overwrite

resource_registry:
 "OS::Nova::Server::Cirros": "cirros.yaml"
